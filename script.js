// Add event listener to animate text on scroll
window.addEventListener('scroll', function() {
  const animatedText = document.querySelector('.animated-text');
  animatedText.classList.add('animate');
});
